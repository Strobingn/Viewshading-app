package com.viewshed.app.viewshed

/**
 * AR View skeleton - camera overlay with viewshed.
 * Ready for CameraX + Compose integration.
 */
object ArViewHelper {
    fun createArOverlay(observer: GeoPoint, visiblePoints: List<GeoPoint>): Any {
        // TODO: Integrate CameraX + overlay composable
        // Return data needed for AR rendering
        return visiblePoints
    }
}
